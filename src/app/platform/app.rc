// Windows resource: embeds the application icon into the .exe so it shows in
// Explorer and the taskbar. Compiled into the game target on MSVC/MinGW only.
// The lowest numeric icon ID becomes the executable's default icon.
1 ICON "icon.ico"
